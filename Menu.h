#pragma once
#include <string>

using namespace std;
class Menu
{
public:
	Menu();

	const char* CrearDisco(const char*);
	void MenuPrincipal();
	string comando(string);
	string nombre(string, int);



};

